---
type: 书写契约
scope: 方法论库（跨案层 ＋ 单案层）
version: v1.2
updated: 2026-09-19
revised: 2026-09-19（§5／§8 与 §6 口径勘误：`本案实证` 禁用面限定为「仅跨案层」）
---

# 条目与回写书写契约

> **一句话**：回写产出的内容与库内既有知识必须**同规**——同一字段、同一标签、同一来源标注；本契约是这一"同规"的**单一事实源**。
> **裁定**：2026-09-19 用户裁定 D1–D5（全部回改／标签收敛双轨／标签去日期／来源统一前置／契约落本文件）。机制侧（触发·流向·顺序·三过滤）见 ADR-0007 与《回写机制统一设计》。

## 1 适用范围与排除

| 范围 | 是否适用 | 说明 |
|---|---|---|
| 跨案层：`通用方法论_*域.md`、`行业方法论_*.md`、`投行语言专项_W/P系列.md`、`通用方法论_投行知识与写作范式.md` | ✅ 全量 | 使用面，检索与援引的对象 |
| 原料层：`单案/*.md`、`行业方法论_单份细分版/*.md` | ✅ 字段名部分 | 蒸馏产出，回写的取材来源 |
| **内容分卷**：`分卷/投行语言专项_P系列_卷*.md`（PL-）、`分卷/通用方法论_体例域_卷*.md`（S-） | ✅ **A2＋A4**（不跑 A1/A3） | **手写条目正文的唯一存放地**（2026-09-16 拆分外置；2026-09-19 纳入索引与检查面——此前 718 条在目录/索引中为 0 条）。分卷内字段自成一套（语用／方法／规则要点／互见），故 A1/A3 不适用 |
| 生成物：`方法论_条目标题目录.md`、`方法论调用索引.md`、`通用方法论_最终版.md` | ❌ 不手改 | 由脚本或源文件重新生成 |
| 历史：`notes/`、`archive/` | ❌ 不动 | 历史即历史，不改写 |

## 2 实证项：四字段模型

**一个「实证项」＝ 四字段**，与排布无关 —— 机器解析只认字段，不认排版。

| 字段 | 必填 | 规范 |
|---|---|---|
| **案名** | ✅ | **规范案名 ＝ 证券简称**（与单案 `case` 字段一致，如「中科仪」；未上市／在审企业取申报文件中的公司简称）——**禁**公司全称、**禁**截断简称（略去通名后缀，如「微容」≠「微容科技」）、**禁**内部代号；不得省为「本案」。白名单与登记见 §2.1 |
| **案号** | 建议 | `AN00NN`；用于反查单案文件与回写清单 |
| **内容** | ✅ | 该案在本条主题下的**做法/数据/口径**（陈述式；禁评价性论证与形容词堆砌） |
| **来源** | ✅ | 见 §4；数字须与来源同源（同一句内禁跨来源混列） |

### 2.1 案名白名单（2026-09-19 用户裁定、落地 A4）

> 用户原话：「案名一定要用规范的案名，用证券简称。」

- **单一事实源**：库根《`方法论_案名规范表.md`》——表一（有单案文件）／表二（无单案文件的早期案）构成**白名单**；**表三 ＝ 别名映射（库内旧写法 → 规范案名）**，仅供回改脚本使用、**不入白名单**（别名一旦出现在标签里即报 ERROR）
- **核定口径**（2026-09-19 用户裁定「核定后修改，核定不了就删了那个简称」）：已上市／挂牌取**证券简称**；在审取**交易所 IPO 项目页「公司简称」字段**（当日实例：云深处→「云深处」、上海超硅→「上海超硅」、兆芯→「兆芯集成」、维安→「维安股份」、鑫华→「鑫华科技」、盛合→「盛合晶微」）；**核不出来的一律从表中删除**，不留悬案
- **适用范围**：库内**一切案名引用位**——实证标签 `**<案名>实证**`、并列清单 `（案名）`、`案名/案名`、族条目尾注 `（案名）`、表格「案名」列、正文指代
- **落地口径**：① 新案蒸馏**先登记规范案名**再写条目；② 白名单外案名引用 → A4 报 ERROR；③ 疑似截断（某白名单案名的真前缀）→ A4 报 ERROR 并给出规范名；④ 标签内写死案数（`三案实证`）→ A4 报 ERROR
- **校验**：`python scripts/check_entry_contract.py --methods-root <工作区根>`（A4；白名单为单案 `case` 字段 ＋ 登记表的并集）

## 3 三种排布（同一数据，三种呈现）

**排布 1 · 列表式（默认）** —— 适用：≥1 案且单案内容 ≤2 行
```
**实证**：
- 环能涡轮（AN0019）——FOB/CIF/CFR「孰晚」确认时点，占比 FOB 49.20%/EXW 22.18%（招 P152；问1 P176-177）
- 森峰激光（AN0021）——出口数据差异率 0.76%/1.02%/0.16%（问1 P216-217）
```

**排布 2 · 独立段式** —— 适用：单案增量 ≥3 行，或含子结构（多级列表/表）
```
**<案名>实证**：
- 细项一……（来源）
- 细项二……（来源）
```

**排布 3 · 表格式** —— 适用：≥3 案且需逐案列出处（列名固定）
```
**实证**：

| 案号 | 案名 | 实证 | 出处 |
|---|---|---|---|
| AN0042 | 三协电机 | …… | 招 P14；问1 P182 |
```

## 4 来源标注契约

**格式**：`（来源简称 + 空格 + P + 页码）`；多来源用 `；`，页码区间用 `-`、并列用 `、`

| 简称 | 指代 |
|---|---|
| `招` | 招股说明书（默认最新版） |
| `招·注册稿` | 需区分版本时 |
| `问1` / `问2` | 第 1／2 轮问询回复 |
| `上会` | 上会稿 |
| `反馈` | 反馈意见回复 |
| `回复` | 问询回复**轮次无法判定时的兜底**（优先用 `问1`／`问2`；2026-09-19 分卷回改时登记） |

**四条规则**
1. **来源在前、页码在后** —— 禁 `（PAGE 195、220 招股书）` 这类颠倒形态
2. **同括号同源** —— 一个数字只挂它实际出处；跨来源须分别标注
3. 多来源用 `；` 分隔；`、` 只用于页码并列
4. 页间分隔统一 `、`（禁 `/P109` 形态）

## 5 标签禁用四条

| 禁用 | 理由 |
|---|---|
| `本案实证`（**仅限跨案层**） | 跨案层条目下「本案」**无指代对象**（库是跨案层，没有"本案"）⇒ 跨案层用 `实证`；**单案层与产出层（`单案/`、`cases/*/产出_*`）不适用本条——那里的规范名就是 `本案实证`**（§6，D6-1 规范优先） ⚠️ 原表述「为两层字段一致，统一为 `实证`」为 D1 旧口径，已被 D6-1 取代（2026-09-19 勘误） |
| 标签内**写死案数**（`（五案）`／`（5 案）`） | 案数随回写变化，写死即失真（用户 2026-09-19 裁定） |
| 标签内出现**日期／「回写」／批次** | 动作与时序不是知识属性；可追溯性由单案文件 ＋ 蒸馏笔记承载 |
| 标签括号内**塞案名**（`（易事达）`） | 案名是字段不是标签；同一案应落为 `**<案名>实证**`（排布 2） |

## 6 单案层字段集（D6-1 · **规范优先**）

**权威源＝`distill-methods.md` §S4 五要素 ＋ §S5 三要素**（案例层模板，本契约**不改写**规范，只登记扩展）

| 字段 | 归属 | 出处 |
|---|---|---|
| `规则要点` | 知识条目 | §S4 五要素 |
| `本案实证` | 知识条目 | §S4 五要素 |
| `可复用结论` | 知识条目 | §S4 五要素 |
| `对照锚点` | 知识条目 | §S4 五要素 |
| `投行语言特征` | 范式条目 | §S5 三要素 |
| `原文实证` | 范式条目 | §S5 三要素 |
| `适用场景` | 范式条目 | §S5 三要素 |
| `句式模板` | **扩展**（本契约登记） | — |
| `边界条件` | **扩展**（本契约登记） | — |

**强制面收缩原则**：契约只约束上述 **9** 个字段名；其余 `- **X**：`（内容小标题如 `周期性`／门禁标记 `G1…G4`·`R1…R4`·`AP-W`／元信息如编号引用）一律视为**非字段**，**不做检查** —— 否则长尾会逼出上百条无意义规则。

### 6.1 为什么单案层是「本案实证」、跨案层是「实证」

| 层 | 写法 | 依据 |
|---|---|---|
| 单案层（案例层） | `本案实证` | 「本案」**有指代对象**（即该案）；且该名是 §S4 冻结模板原文 |
| 跨案层（主库） | `**实证**` ／ `**<案名>实证**` | 规范**未定义跨案层字段**（§S4 只定义案例层条目模板）；主库是跨案层，「本案」无指代 ⇒ 由本契约定义 |

**实测分布（2026-09-19 · 三层实测，作为「异名是设计」的证据）**：

| 层 | `实证`（短式） | `本案实证`（全式） | 规范名 |
|---|---|---|---|
| 主库根（跨案层） | **224** | **0** | `实证` / `<案名>实证` |
| 单案层 `单案/` | 0 | **1,490** | `本案实证` |
| 产出层 `cases/*/产出_*` | 0 | **2,404** | `本案实证` |

**结论：跨层传递不需要"名字映射"表** —— 回写从单案取内容落为主库实证项，本就是**结构搬运**（案名成为字段、页码转契约格式），字段名差异不构成额外成本。（勘误：2026-09-19 首版契约曾把「两层异名需映射」列为"规范优先"的代价，属**夸大**，已纠正。）

## 7 回写产出三条硬约束

1. **只用 §3 的三种排布**，不得自创标签形态（历史反例：`**<案名>实证（YYYY-MM-DD 回写）**` 曾作为纪律写入 SKILL.md，已按 D2/D3 废止）
2. **标签内禁「回写」「日期」「批次」** —— 留痕走 CHANGELOG ＋ 蒸馏笔记
3. **落法与重合度挂钩**：高重合 → 并入既有实证区（字段同规，不改案例列表结构）；中高 → 排布 2 独立段（不并入、不改案数表述）

**校验链**：落盘脚本 dry-run 阶段必跑 `scripts/check_entry_contract.py`（**0 ERROR 才允许 `--apply`**）；收尾必跑 `refresh_index.py` ＋ `check_methods_health.py`。

## 8 存量处置（2026-09-19 · D1 全量回改已完成）

| 项 | 处置 | 结果 |
|---|---|---|
| 标签五态归一（本案实证／案数／案名／日期／回写后缀） | 全量回改 | ✅ 残留 0 |
| 来源标注顺序与简称 | 全量回改（跨案层） | ✅ 可判定部分全改；残留裸 PAGE 登记 |
| 单案层两条字段名（**D1 旧口径·已被取代**） | 全量回改后**回改回全式** | ⚠️ D1 轮曾把单案层改为短式（`规则要点`→`规则`、`本案实证`→`实证`）；**D6-1 用户裁定「都保持规范的状态」后按规范优先回改为全式** —— 现状（2026-09-19 实测）：单案层 `本案实证` **1,490 处**／`cases/` 产出层 **2,404 处**；跨案层维持短式 `实证` **224 处**（依 §6.1，**合规、不回改**） |
| **单案层短式残留归零（2026-09-22）** | 全量回改（只改 `单案/`，**绝不动跨案层短式**） | ✅ 已归零 | 开发对话按用户裁定「都按你建议来」执行：`单案/` 下 **10 个文件 / 312 处** `- **实证**` → `- **本案实证**`；脚本＝`scripts/…`（一次性）＋改前备份 `workflow/state/backup_20260922_methods_shortform/`（10 文件）；复验：单案层短式残留 **0**、行尾守恒、字节增量 ＝ 6×处数 全部通过 |
| 累计改动 | 3 轮 · **3,895 处次**（含自我修复 108 处） | 备份见 `_backup/contract-20260919{,b,c}` |
| 生成物（分卷）与单案层来源标注 | **未动** | 分卷待重新生成；单案层来源标注待 D6-2 |
| **单案层字段标签归并（D6-1）** | 全量回改 | ✅ **146 种 → 9 种**（别名映射 5,113 处落盘；旧写法残留 0；非字段 290 处未动） |
| **案名规范（证券简称 · A4）** | 全量回改 ＋ 建白名单 | ✅ 截断案名 **174 处标签 ＋ 362 处案名引用位 ＋ 22 处「案名+案」＋ 18 处案数词标签** → 共 40 文件；幂等重跑 0 命中；《方法论_案名规范表.md》建（表一 53／表二 7／表三待确认 13）；备份 `archive/_backup-casename-20260919` |
| **内容分卷纳入检查面（2026-09-19）** | A2＋A4 全量回改 | ✅ 分卷旧式来源标注 **33 处**（体例域卷1：问N P 15／招·注册稿 P 3／招 P 8／回复 P 6 ＝按行 15 行）＋ P 卷 2 处说明语去标签化 ＋ 繁体字修正；**A2 规则补两条**（「来源名＋PAGE」「轮次＋PAGE」——原规则抓不到 `AGE` 形态）；简称表补 `回复` 兜底 |
| **718 条分卷条目纳入索引（2026-09-19）** | 生成器扩展 | ✅ `parse_titles`／`gen_entry` 纳入 `分卷/` ＋ 支持 `PL-`·`S-`；目录与索引各增「**附：P 系列**」（442 条，含卷文件＋行号）／体例域分卷段落；路由入口域路由表纳入分卷（此前 **0 条** → 现 442＋299） |

**回归护栏**：本轮回改中自身引入过一类写入损坏（`招·注册稿` 被二次加前缀成 `招·招·注册稿`，10 文件 84 处）——由**落盘前预测文本自校验**抓住并修复；该守卫已固化进归一脚本（`RESIDUAL` 三项）。单案层归一同源实现三道自校验（**别名残留 0／标签数守恒／行尾守恒**），全部在落盘前判定。

> **⚠️ 未纳入本次归一的「短式」位置（2026-09-22 实测 · 逐项复核后订正）**：① `行业方法论_单份细分版/` **29 处／3 个文件** —— 该层不在本契约层表内（层表只列 `单案/` 与 `cases/*/产出_*`）⇒ 归属待裁定。**实测形态是「同层异名」**：同目录 27 个文件已用全式（如 九目化学 28 处、凯达重工 27 处、安达股份 28 处），仅 3 个文件仍用短式（华晟智能 14 ／ 天元重工 12 ／ 许昌智能 **3 短＋5 全 同文件混用**）⇒ 系**批次差**而非模板差异，**未动**（待层归属裁定后一次性归一）。② `分卷/通用方法论_体例域_卷3_B5.md` **6 处** `- **实证**：` —— **复核后订正：这是合法字段用法，不是残留**（该字段后跟**嵌套子列表**列各案实证，形如 `- **实证**：` ↓ `  - 南方乳业（AN0080）——…`）⇒ 真正的问题是 **§9「分卷条目字段」清单漏收 `所述族`／`实证` 两个字段名**，属**契约册清单不全** ⇒ **已于 2026-09-22 补全**（详见 §9 末两注），**数据不动**。③ `notes/` 45 处（过程笔记）与 `archive/` 856 处（历史快照）**按「存量归档不改」原则不动**。

## 9 维护

- 改本文件 ＝ 改契约（属规范变更）→ 记 `ibd-methods-ops` CHANGELOG，重大变更记 ADR
- 自检脚本：`python scripts/check_entry_contract.py --methods-root <工作区根> [--json]`
- 归一脚本（一次性，幂等）：`<工作区>/tasks/_contract_normalize_20260919.py`

> **分卷条目字段（2026-09-19 补 · 2026-09-22 补全）**：`来源案`（`- **来源案**：<案名>（AN00xx）｜`，与「语用」同行）＋ `语用`／`方法`／`规则要点`／`原文实证`／`互见`／**`所述族`**／**`实证`**。`来源案` 为**块级归属**，作用是为「原文实证」中的裸 `PAGE N` 提供案级上下文，并作为该块内 `S-AN{案号}-NN` 类引用的归属依据。
>
> ⚠️ **2026-09-22 补全说明（承 §8 注 ②）**：`所述族`（实测 12 处）／`实证`（实测 6 处）**仅出现在** `分卷/通用方法论_体例域_卷3_B5.md`。`实证` 是**带嵌套子列表的块级字段**——`- **实证**：` 之后跟 `  - <案名>（AN00xx）——…`，**不是空值标签、不是模板占位**；`所述族` 与之同族（`- **所述族**：01 结构与版式`）。者二皆属**合法字段用法 ⇒ 数据不动**，此前 §9 清单只列前五者，致 2026-09-22 首次复核时误判为「短式残留」，现补全。



---

## 附：S7 回写执行纪律（12 条 · 自 `SKILL.md`「关键要点」原样迁入 · 2026-09-21 瘦身）

> 落点选本册的理由：本册是「条目与回写书写契约」单一事实源，该纪律 ①②③⑧⑩ 均为契约的执行侧落点；**原 `SKILL.md` §S7 步骤仍保留并内联指向本节**。

- **S7 回写执行纪律（2026-09-19 首两批实战沉淀）**：①**起草前先汇编**——跑 `gen_replay_worksheet.py --case N` 把「清单候选 × 单案新条目 × 主库旧条目」三方拉齐再动笔（免去逐条开文件的来回读）；②**书写形态遵循契约**（单一事实源 → `references/entry-contract.md`）——实证项字段＝**案名＋案号＋内容＋来源**；高重合＝并入既有「**实证**」区（列表式，一案一项）；中/中高＝排布 2 独立段 `**<案名>实证**：`（**不得带日期、不得带「回写」字样**——动作与时序的留痕走 CHANGELOG 与蒸馏笔记）；**标签内禁案数/日期/案名**（历史反例：`**<案名>实证（YYYY-MM-DD 回写）**` 曾写在本纪律里，2026-09-19 按契约 D2/D3 废止）；③**落盘脚本块级锚点**——锚点用条目标题＋「**适用场景**」行，dry-run 预检必须 **0 问题**才 `--apply`；**幂等检查须限定在本条目块内**（跨条目查同名前缀会误报已回写）；④完成后**必跑** `refresh_index.py` ＋ `check_methods_health.py`（0 ERROR）并在回写清单逐条销项；⑤索引刷新使行号漂移，**抽验时窗口要覆盖到「适用场景」行**（实证列表型条目可长达 60 行，窗口过窄会误报 MISS）⑥**引用与数字须来源对齐**——回写实证的数字往往跨来源（招股书正文／问询回复／问询多轮），**同一句内的数字必须同源，不同来源要分别标注**；回原文抽验时按「数字能否在引用所指文件里命中」判定（2026-09-19 实证：某条把问询回复的「16.83%/10.14%/8.61%（2022-2024）」与招股书「招PAGE 8」并列，而招股书报告期口径实为「10.14%/8.61%/10.44%（2023-2025）」——两口径自洽但期间不同，混列会误导）⑦**案数不写死数字**（用户 2026-09-19 裁定「不需要确切数字」）——统一用「多案验证／多案共通」，禁「N 案验证」写法（历史维护不实，且每轮回写都在给不准的数字 +1）⑧**契约自检前置**——落盘脚本 dry-run 阶段必跑 `python `scripts/check_entry_contract.py` --methods-root <工作区根>`（检查项：实证标签是否属契约白名单／来源标注是否「来源在前」／单案层字段名是否为 `规则`·`实证`），**0 ERROR 才允许 `--apply`**；把「形态错误」从"事后抽验"提前到"落盘前拦截"（2026-09-19 起）⑨**案名一律用规范案名（证券简称）**——用户 2026-09-19 裁定「案名一定要用规范的案名，用证券简称」：库内**一切案名引用位**（实证标签 `**<案名>实证**`、并列清单 `（案名）`、族条目尾注、表格「案名」列、正文指代）必须用《`方法论_案名规范表.md`》登记的规范案名；**禁**全称／截断简称（「微容」须写「微容科技」）／内部代号；新案**先登记再写条目**；A4 门禁校验（截断·未登记·写死案数 → ERROR）；**核定口径**（2026-09-19 用户裁定「核定后修改，核定不了就删了那个简称」）＝已上市/挂牌取**证券简称**、在审取**交易所 IPO 项目页「公司简称」字段**，**核不出来的一律从表中删除、不留悬案**；表三＝**别名映射（旧写法 → 规范案名）**，仅供回改脚本使用、**不入白名单**。⑩**规范类变更先于内容回写**——案名规范／字段名／标签形态／来源简称等**规范面**的调整，须在批次回写**之前**收口；回写后再补做规范会引发全量二次回改（2026-09-19 实证：先回写 47 条、后案名规范化 ⇒ **384 处二次回改**）。落盘脚本 dry-run 阶段内置 A1–A4 自检，即本纪律的机器侧兜底。⑪**改库前的并发自检**——动手前先 `git status` 查脏（同目录可能已被另一会话改动）；**同一域文件同日只由一条会话改**——跨会话并发无锁，§4.8 的 hunk 手术只能救不重叠区，**同段冲突不可恢复**；`git add` 只加自己改的路径，**禁 `add .`**。⑫**笔记追加须留痕**——蒸馏笔记后续追加内容（如 S7 回写实战小节）时，同步在头部加/更新 `> **最后更新**：YYYY-MM-DD（动作）` 行：文件名日期是**蒸馏批次日**，不等于内容最后修改日，缺此行会让检索误判「该轮未做后续动作」（2026-09-19 实证：`2026-09-05_中科仪_蒸馏笔记.md` 内含 09-19 追加的回写小节）。
