# 护栏体检详表（维护域 · 步骤 1）

> 脚本单一事实源：`scripts/check_methods_health.py`（2026-08-31 治理 D 档建立）。蒸馏 S7 第⑤步自动执行（沉淀后门禁）；维护域按需手动执行。

## 用法

```
cd {METHODS_ROOT}
python scripts/check_methods_health.py         # 全量检查（打印逐项结果）
python scripts/check_methods_health.py --quiet # 安静模式（仅 exit code，供自动化判断）
```

## 8 项体检清单

| # | 检查项 | 判定 | 漂移时修复入口 |
|---|--------|------|---------------|
| 1 | frontmatter 完整性 | 全部活跃文件须 `---` 定界闭合 + 字段 `key: value` + frontmatter 后接 `#` 标题 | `add_frontmatter.py`（根目录）/ `add_fm_single.py`（单份细分版） |
| 2 | 空 h3 | h3 标题下不得无内容（紧跟 h3/h2/EOF） | 手工补内容或删除空标题 |
| 3 | 编号连续性 | 域文件 (N) 体系 1..N 连续，无缺口、无倒挂；v37 体系含跨文件唯一性 + 族内连续 + 登记表一致 | `b_fmt_unify.py`（结构归一） |
| 4 | 路由表计数 vs 实算 | 入口路由表「N 条」与 parse 实算一致 | 重跑 `gen_entry_p0.py`（`refresh_index.py` 内含） |
| 5 | parsed TOTAL vs 实算 | parsed_titles 总数与 h3/W 条目实算一致 | 重跑 `parse_v26_titles.py` |
| 6 | W 编号唯一性 | W 系列条目编号零重复 | 手工核对重复条目（283 条基线） |
| 7 | 交叉引用有效性 | 正文引用 `[FLIW]L?-\d{6}` 目标编号必须存在（旧 W-00xxxx 历史注记跳过；2026-08-31 D6 裁定新增） | 核对登记表/迁移，改引用或补条目 |
| 8 | 域文件体积警戒线 | 单域文件 >300KB 触发 WARN（不阻塞）+ 族级拆分预案提示（2026-09-03 新增，防二阶膨胀复发；当前最大=财务域 235KB） | 按登记表最大族外置条目入 archive/ + 路由表登记指针 |

## 排除目录与文件（合法无 frontmatter，护栏跳过）

- 目录：`notes/`（复盘笔记）、`archive/`（归档；含 `legacy_v34v35_backup_20260901` 等历史世代快照——2026-09-01 起原 `_backup/` 并入 archive/）
- 自动生成文件 4 个：README / 方法论_条目标题目录.md / 方法论调用索引.md / 行业方法论_合并映射.md（注入会被生成脚本覆盖）；「调用索引备份」「编号登记表（v36 遗留）」2026-09-01 已迁 archive/，不再属活跃区

## 历史实证（护栏价值 · 2026-08-31 首跑抓到的三类漂移）

1. **路由表计数漂移**：W 系列路由表 16 条 vs parse 实算 283（267 条列表条目漏解析 + 10 个归组章节头误判）
2. **frontmatter 缺失**：行业方法论_单份细分版/ 15 个活跃文件无 frontmatter（首轮补齐只覆盖根目录）
3. **归档目录误报**：_backup/、notes/ 被强制要求 frontmatter——排除目录规则补上后收敛

防复发场景：C 档新增附录若被 parse 误计为条目（写作域 94→97 误报）、蒸馏新案后编号不连续——护栏 3/4/5 项即拦截。

## 归档策略（2026-09-01 定 · 维护域执行）

- **统一位置**：`methods/archive/`——所有历史版本快照、迁移备份、过时文件一律入此；**铁律全保留**（只迁不删）
- **命名规范**：文件 `[描述]_[YYYYMMDD].md`（如 `编号登记表_v36遗留_20260901.md`）；目录 `[描述]_backup_[YYYYMMDD]`（如 `legacy_v34v35_backup_20260901`）
- **世代标签**：跨版本快照带世代标识（v34/v35/v37…），避免与现行编号体系混淆
- **删除底线**：archive/ 内文件不主动删除；确需清理先 sha256 去重 + 留删除清单再动
- **体检联动**：本表「排除目录」节与 archive/ 实时同步；每次维护域体检顺带核对 archive/ 无重复残留
