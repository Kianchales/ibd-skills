<p align="center">
  <img src="https://img.shields.io/badge/IBD%20Methods%20Query-%E6%96%B9%E6%B3%95%E8%AE%BA%E5%BA%93%E6%A3%80%E7%B4%A2-2e6cc4" alt="IBD Methods Query">
</p>

<p align="center">
  <img src="https://img.shields.io/badge/IBD%20%E6%96%B9%E6%B3%95%E8%AE%BA%E6%A3%80%E7%B4%A2-blue" alt="displayName">
  <img src="https://img.shields.io/badge/version-0.5.0-green" alt="version">
  <img src="https://img.shields.io/badge/License-MIT-blue" alt="MIT">
</p>

# ibd-methods-query

方法论库「检索域」skill——把「索引 → 定向读取 → 合成」固化为标准检索链路，按需只读命中条目，**绝不全文读库**。

## 💡 这是什么

写作/分析/问答要查方法论时走本 skill 的标准链路：问题拆解 → 索引定位 → 定向读取 → 合成输出。库被索引映射为「编号 → 域文件 + 行号」，单次读取仅约 20KB。

## ✨ 快速开始

```
「查XX问题怎么写」
→ S1 问题拆解（问题域编号 + 关键词）
→ S2 索引定位（速查表 → 条目编号 → 条目标题目录得「域文件 + 行号」）
→ S3 定向读取（Read offset/limit 只读命中条目）
→ S4 合成输出（写作素材 / 分析结论 / 引用清单）
```

## 🧩 核心能力

- **四层检索对象**：主库域条文 → 语言专项（W 系列 / P 系列分卷）→ 行业版（合并版 + 单案叙述型行业研究章）→ 单案（范式文件）
- **三种输出形态**：回复写作素材（给 writer）/ 分析结论（给分析角色）/ 引用清单（交付门禁）
- **检索失败降级**：速查无匹配 → Grep 关键词 → 登记待沉淀（新方法论缺口）
- **冷启动引导（首次使用友好）**：库还没建？不报错——引导你确认库路径、自动建好空库骨架和最小配置，本次如实告知「无命中」并登记待沉淀，然后就能边用边长

## 🚀 典型场景

- **第一次用（还没建库）**：直接问「查 XX 问题怎么写」→ 触发冷启动引导，一问定路径、自动建库骨架，之后再查就进正常链路
- **写反馈回复前**：查「这类问题以前怎么答得好」——按问题域定位条目，定向读取
- **分析 / 问答消费方法论**：把命中条目汇总为分析结论或写作素材
- **交付前引用清单**：条目编号 + 使用位置对照（引用自检）
- **库很大时避免全文读**：索引把库映射为「编号 → 域文件 + 行号」，单次读取仅约 20KB

## 🔗 与生态内其他 skill 的分工

- `ibd-methods-ops`：互补——ops 管生产+维护（写入侧），本 skill 管检索消费（读取侧）
- `ibd-doc-write`：供其可选调用（写作链路的查库环节）；write 无本 skill 时按内嵌流程运行

## 📦 安装与依赖

拷贝至用户级技能目录。依赖：

- **方法论库 `{METHODS_ROOT}`**（使用者自建，路径可配置，示例 `~/methods/`）及其索引资产（检索入口：**调用索引 / 条目标题目录 / 路由入口**——由 `ibd-methods-ops` 的 `refresh_index.py` 生成；按案名检索另用库内「案名规范表」，单案侧另有 `state/单案索引对照表.md`，位于**工作区根**而非库根）
- 工具仅内置 `Read` / `Grep`，零第三方依赖

## 📁 目录结构

```
SKILL.md                            检索流程 SOP（S1-S4）+ 索引资产表（薄壳，详规下沉）
references/retrieval-chain.md       检索链路细节（§1-§6）+ 四层/索引资产/输出形态/关键纪律/边界/维护原文（§7-§12）
references/library-backend.md       库后端接入（A/B 档）+ 依赖与缺失降级三态 + 冷启动引导档
examples/response-retrieval.md      反馈回复全流程演示
CHANGELOG.md                        版本记录
```

## 📌 近期更新

- **2026-09-20 · v0.4.0**：**检索层对齐库实况**——① 检索对象三层 → **四层**（拆出「语言专项：W 系列 / P 系列分卷」，P 系列 442 条与体例域批次正文已外置 `分卷/`）② 行业版路径修正（**扁平 `行业方法论_<类名>.md`，无「合并版」子目录**）③ 单案路径补 `单案/` 归拢层，单案索引对照表定位明确到**工作区根 `state/`** ④ 索引资产表去 `编号登记表.md`（该资产 2026-09-01 退役迁 archive，编号唯一性改由调用索引全量映射 ＋ 条目标题目录保障）⑤ 案号形态更正为 **4 位 AN**（下批 AN0076）、「无单案文件案」由 8 案更正为 **22 案** ⑥ 补「案名一律用证券简称」纪律（白名单源＝库内案名规范表）与字段契约对齐（跨案层 `实证`／单案层 `本案实证`）⑦ 新增行号漂移自检（`check_index_locator.py`）
- **2026-09-18 · v0.3.0**：新增**冷启动引导档**——库未建/配置缺失时不再「索引读取失败」报错：一问定路径 → 按 ops methods-guide 模板建空库骨架（4 文件）→ 生成最小 `library.config.json` → 交付「无命中 + 待沉淀登记」。降级链重构为三态（正常 / Grep 兜底 / 冷启动引导）；ops「边界与协作」同步冷启动分工裁定
- **2026-09-18 · v0.2.1**：接入点声明补第三问**缺失降级**（索引缺失→Grep 全库扫描兜底档 / 调用方另有内嵌流程 / 零脚本无运行时依赖）——本包正式入 `ATTACHMENT-POINTS` 总表（公开包口径 5→7）后，满足 P2+ 三问齐备要求
- **2026-09-10 · v0.2.0**：公开首版——索引资产「使用者自建」模型；按能力档位适配（A 文件型走行号定向 / B 检索型走连接器搜索）
- 更早版本见 [CHANGELOG.md](CHANGELOG.md)

## ⚖️ 许可

MIT
